apiVersion: v1
kind: Service
metadata:
  name: llm-service
  namespace: techtrek
spec:
  selector:
    app: llm
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.port }}
  type: ClusterIP