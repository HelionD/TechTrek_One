apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: myingress
  namespace: techtrek
  labels:
    app.kubernetes.io/name: myingress
spec:
  rules:
  - host: onealbania-frontend-23052026.web.app
    http:
      paths:
      - pathType: Prefix
        path: "/"
        backend:
          service:
            name: backend-service
            port:
              number: 8000