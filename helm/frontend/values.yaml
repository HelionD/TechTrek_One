image:
  repository: helion444/frontend
  tag: v25
  pullPolicy: IfNotPresent
  namespace: ""

replicaCount: 1

ingress:
  host: "localhost"
  className: "nginx"

service:
  type: ClusterIP
  port: 80

containerPort: 80